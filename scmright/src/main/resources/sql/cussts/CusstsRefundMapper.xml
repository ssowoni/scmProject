<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.happyjob.study.cussts.dao.CusstsOrdRefDao">

<!-- 반품 목록 조회 
<select id="selectRefund"  resultType="kr.happyjob.study.cussts.model.CusstsRefundModel">
	 select tr.retNo
			,tr.coNo
	        ,tp.prodName
	        ,tr.retADate
	        ,tdc.detail_name
		 from tb_retList tr
		 inner join tb_detail_code tdc on tdc.detail_code = tr.retSts
		 inner join tb_cOrder tc on tc.coNo = tr.coNo
		 inner join tb_product tp on tc.prodNo = tp.prodNo
	 <where> 
	 and  tr.loginID = #{loginID}
	 and group_code = 'refundCD'
     </where>
 
</select>
-->

<!--반품 목록 개수 조회  
<select id="selectRefundCnt" resultType="int">
    	 select count(*)
		 from tb_retList tr
		 inner join tb_detail_code tdc on tdc.detail_code = tr.retSts
		 inner join tb_cOrder tc on tc.coNo = tr.coNo
		 inner join tb_product tp on tc.prodNo = tp.prodNo
	 <where> 
	 and  tr.loginID = #{loginID}
	 and group_code = 'refundCD'
     </where>
</select>
 -->
</mapper>